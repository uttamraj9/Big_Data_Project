{
  "type": "ScheduleTrigger",
  "description": "Manual/on-demand trigger for full load. Disabled by default — start it manually when you need to reload all data from PostgreSQL.",
  "typeProperties": {
    "recurrence": {
      "frequency": "Month",
      "interval": 1,
      "startTime": "2024-01-01T00:00:00Z",
      "timeZone": "UTC"
    }
  },
  "pipelines": [
    {
      "pipelineReference": {
        "referenceName": "PL_CC_Fraud_Trans_PG_To_Raw",
        "type": "PipelineReference"
      },
      "parameters": {
        "load_type": "full"
      }
    }
  ]
}
